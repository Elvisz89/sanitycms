import { eventType } from './eventType'
import { homepage } from './homepage' 
import { product } from './product'  // Import the product schema
import { accessory } from './accessory'  // Import the product schema

export const schemaTypes = [eventType, homepage, product]  // Add product to the schemaTypes array
